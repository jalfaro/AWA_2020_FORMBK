CREATE TABLE `awa`.`formulario` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `nombre` VARCHAR(80) NULL,
  `fecha_nacimiento` DATE NULL,
  `sexo` INT NULL,
  `fecha_creacion` DATETIME NULL,
  PRIMARY KEY (`id`));
